level: $LEVEL
debug: $DEBUG
prefix: $PREFIX
tenant: $TENANT
auth:
  enabled: false
  required:
    - roles:personnel

eventstore:
  host: $EVENTSTORE_HOST
  port: $EVENTSTORE_PORT
  login: $EVENTSTORE_LOGIN
  password: $EVENTSTORE_PASSWORD